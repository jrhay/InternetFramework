<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Company>Portable Knowledge, LLC</Company>
    <Description>.NET Standard implementation basic sockets and Internet RFC-defined protocols (Telnet, etc) in fully native .NET event-driven async models</Description>
    <PackageProjectUrl>https://github.com/jrhay/InternetFramework</PackageProjectUrl>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>

</Project>
